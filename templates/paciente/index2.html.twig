<h1>Pacientes con móvil</h1>

<ul>
    {% for key, paciente in pacientes %}
        <li><a href="{{ path('detalle', {'nombre': key, 'apellido': paciente[0], 'telefono': paciente[1]}) }}">{{ key }} {{ paciente[0] }}</a></li>
    {% endfor %}
</ul>